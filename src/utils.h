#ifndef UTILS_H
#define UTILS_H


/*--- Function that prints error message and exit through system calls perror() and exit() with exit status EXIT_FAILURE ---*/
void perror_exit(char *message);


#endif //UTILS_H